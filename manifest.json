{
  "name": "Notify if visited URL already",
  "description": "Notify if visited URL already",
  "version": "1.2",
  "background": { "scripts": ["background.js"] },
  "browser_action": {
      "default_icon": "notvisited.png"
  },
  "permissions": [
    "history",
	"tabs",
	"webNavigation"
  ],
  "manifest_version": 2
}
